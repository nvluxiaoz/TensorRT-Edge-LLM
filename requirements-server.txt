# Runtime-only server dependencies. Install the project extras `server-tools`
# for tool chat templates and `native-build` when compiling pybind bindings.
fastapi==0.139.2
uvicorn==0.51.0
huggingface-hub>=1.5.0,<2.0
av==17.1.0
numpy==2.2.6
python-multipart==0.0.32
